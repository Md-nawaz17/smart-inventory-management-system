MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/smart-inventory
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d
CLIENT_URL=http://localhost:5173
PORT=5000
# Optional comma-separated DNS resolvers for environments where Node DNS fails:
# DNS_SERVERS=8.8.8.8,1.1.1.1
